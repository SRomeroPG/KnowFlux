We are bootstrapping the Claude Audit Team for the KnowFlux repository.

This repository already contains:

- the frozen Knowledge Framework Core Specification v2.1;
- AI_STRIKE_TEAM_BOOTSTRAP.md;
- the shared .strike-team/ control plane;
- the Codex Implementation Team configuration.

Your task in this session is ONLY to bootstrap the native Claude audit team.

Do NOT audit KnowFlux yet.
Do NOT modify KnowFlux product implementation.
Do NOT start a development phase.
Do NOT create fake Codex configuration.

────────────────────────────────────────────────────────
AUTHORITIES
────────────────────────────────────────────────────────

1. Knowledge Framework — Core Specification v2.1
   - normative and frozen;
   - SHALL / SHALL NOT are implementation contracts.

2. AI_STRIKE_TEAM_BOOTSTRAP.md
   - defines the two-team development protocol;
   - Codex implements and remediates;
   - Claude independently audits and accepts/rejects.

3. Existing repository-local control-plane files.

────────────────────────────────────────────────────────
TASK
────────────────────────────────────────────────────────

Inspect the Claude Code runtime currently available in this environment.

Determine:

- the native project-scoped subagent mechanism;
- whether custom subagents can be persisted in the repository;
- whether model selection per subagent is supported;
- which included models are actually available under the current plan;
- available effort/reasoning controls;
- concurrency behavior;
- tool/permission boundaries;
- whether read-only auditor roles can be enforced natively.

Do not guess capabilities.

Use only mechanisms actually supported by the installed Claude Code runtime.

Then create the Claude Audit Team described in
AI_STRIKE_TEAM_BOOTSTRAP.md.

The root Claude session SHALL act as:

    Claude Audit Orchestrator

Create native specialists equivalent to:

- Specification Compliance Auditor
- Adversarial Architecture Auditor
- Test & Conformance Auditor
- Algorithm & Determinism Auditor
- Integration & Regression Auditor
- Mechanical Audit Worker

The specialists are subordinate to the Audit Orchestrator.
They are not independent project leads.

────────────────────────────────────────────────────────
MODEL ROUTING
────────────────────────────────────────────────────────

Quality is the primary objective.
Token efficiency is secondary.

Under the user's limited plan:

- use Sonnet as the normal audit workhorse;
- reserve Opus for HIGH/CRITICAL reasoning or final adjudication when its
  included quota makes that practical;
- use lower-cost available models for purely mechanical checks;
- do not spend premium capacity on search, grep, enumeration, formatting or
  routine command execution.

If model selection per subagent is not supported, preserve the roles anyway
and control cost using narrower tasks, effort settings and context isolation.

Do not assume unlimited usage.

────────────────────────────────────────────────────────
AUDITOR INDEPENDENCE
────────────────────────────────────────────────────────

The Claude team is an independent auditor.

Normal Claude audit agents SHALL NOT modify product/framework implementation.

Their default behavior is:

- inspect;
- analyze;
- run tests/builds/verifiers;
- reproduce defects;
- write audit artifacts only.

They MAY write under:

    .strike-team/phases/*/audits/
    .strike-team/spec-gaps/

They SHALL NOT edit KnowFlux implementation during normal audit.

The user intentionally wants Codex to perform all fixes.

────────────────────────────────────────────────────────
SMOKE TEST
────────────────────────────────────────────────────────

After creating the native agents:

Spawn at least two harmless audit subagents.

For example:

1. Specification Compliance Auditor
2. Test & Conformance Auditor

Have them independently inspect the repository structure and the frozen spec,
without making product changes.

Each should return a short report proving that:

- it was actually spawned as a subagent;
- it received its audit role;
- it could inspect repository files;
- it did not modify product implementation.

Do not perform a real phase audit yet.

────────────────────────────────────────────────────────
FINALIZE
────────────────────────────────────────────────────────

Update:

    .strike-team/ENVIRONMENT.md

with the Claude runtime facts you actually verified.

Record:

- agent mechanism;
- subagents successfully created;
- models actually available;
- model-selection capability;
- effort controls;
- concurrency;
- relevant quota constraints;
- audit write restrictions.

Then report:

1. exact native Claude agent files/configuration created;
2. agents successfully recognized;
3. smoke-test result;
4. any runtime limitation that affects the Strike Team protocol.

Then STOP.

Do not audit KnowFlux until the user explicitly starts an audit phase.