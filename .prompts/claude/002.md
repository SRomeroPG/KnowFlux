Begin the independent Claude audit of the current KnowFlux Strike Team phase.

Phase:
    {{PHASE_ID}} — {{PHASE_NAME}}

Expected state:
    CODEX_READY_FOR_AUDIT

You are the Claude Audit Orchestrator.

Read:

- frozen Core Specification v2.1;
- AI_STRIKE_TEAM_BOOTSTRAP.md;
- .strike-team/STATE.yaml;
- .strike-team/PHASES.md;
- {{PHASE_PATH}}/PLAN.md;
- {{PHASE_PATH}}/TRACEABILITY.md;
- {{PHASE_PATH}}/CODEX_HANDOFF.md;
- actual implementation;
- actual tests;
- relevant conformance fixture.

Record the exact commit being audited.

Treat all Codex-produced claims as untrusted until independently verified.

Do not modify product implementation.

────────────────────────────────────────────────────────
AUDIT OBJECTIVE
────────────────────────────────────────────────────────

Determine whether this phase:

1. completely satisfies every normative SHALL / SHALL NOT declared in its
   PLAN and TRACEABILITY scope;

2. remains compatible with all previously ACCEPTED phases;

3. does not invent product semantics absent from the frozen specification;

4. does not pull later-phase behavior forward in a partial/inconsistent form;

5. has sufficient positive, negative and boundary verification;

6. evolves the conformance fixture correctly;

7. introduces no architectural constraint that contradicts later normative
   requirements.

────────────────────────────────────────────────────────
AUDIT STRATEGY
────────────────────────────────────────────────────────

Derive the audit strategy from:

- phase risk;
- normative sections covered;
- files/components changed;
- algorithms introduced;
- persistent-format effects;
- integration effects.

Use the configured Claude specialists selectively.

At minimum include:

- Specification Compliance audit;
- Test/Conformance audit.

Add Algorithm/Determinism, Architecture or Integration auditors when the
phase actually warrants them.

Do not waste quota on redundant agents.

────────────────────────────────────────────────────────
TRACEABILITY
────────────────────────────────────────────────────────

Independently enumerate the normative requirements that this phase should
cover.

Compare that enumeration against TRACEABILITY.md.

A missing requirement from Codex's matrix does not remove it from scope.

For each normative requirement determine:

    IMPLEMENTED_AND_PROVEN
    IMPLEMENTED_NOT_PROVEN
    NOT_IMPLEMENTED
    INCORRECTLY_IMPLEMENTED
    NOT_APPLICABLE_WITH_EVIDENCE

────────────────────────────────────────────────────────
FINDINGS
────────────────────────────────────────────────────────

Create stable finding IDs:

    CLAUDE-{{PHASE_ID}}-R01-F001
    ...

Every REQUIRED finding must contain:

- severity;
- spec reference;
- requirement;
- evidence;
- actual behavior;
- expected behavior;
- impact;
- reproduction;
- remediation constraint.

Use SPEC_GAP only when the frozen spec genuinely lacks a semantic decision
required to proceed safely.

Do not invent the missing semantic yourself.

Keep optional design preferences as ADVISORY and do not block acceptance on
them.

────────────────────────────────────────────────────────
AUDIT OUTPUT
────────────────────────────────────────────────────────

Create:

    {{PHASE_PATH}}/audits/CLAUDE_AUDIT_R01.md

Include:

- audited commit;
- scope;
- specialists used;
- commands executed;
- independent normative coverage;
- findings;
- conformance assessment;
- integration/regression assessment;
- residual risk.

End with exactly one:

    VERDICT: CHANGES_REQUESTED

or:

    VERDICT: ACCEPTED

If changes are required:
    update STATE.yaml and STOP.

If accepted:
    update STATE.yaml to ACCEPTED and STOP.

Never begin the next phase.
Never modify implementation during audit.

Begin now.