# Frozen specification examples used by P001 tests

These YAML documents reproduce the §10.3, §16.3, and §16.6 examples from
`knowledge-framework-spec-v2.1.md`. The §16.6 example has only the required
`schema_version: 2.1` header added. Their referenced IDs illustrate schema
syntax; they are not part of the Acme formulation project fixture and do not
claim cross-document referential integrity.

The §23.3 DIV shape is tested only in memory. The conformance project never
hand-authors a derived DIV record.
