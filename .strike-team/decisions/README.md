# Development Decisions

Record durable process or implementation decisions only when the frozen specification already determines the product semantics. Do not use this directory to resolve a specification gap; create a `spec-gaps/SG-xxxx.md` artifact instead.
