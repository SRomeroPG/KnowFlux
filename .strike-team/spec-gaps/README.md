# Specification Gaps

Create `SG-xxxx.md` only when a frozen-spec semantic decision is genuinely missing and blocks an affected workstream. Do not silently resolve gaps in code or use this directory for ordinary defects.
