# Phase Artifacts

Each authorized phase receives a `Pxxx-<phase-name>/` directory containing its `PLAN.md`, `TRACEABILITY.md`, `CODEX_HANDOFF.md`, `audits/`, `remediation/`, and `evidence/` directories. Do not create a phase directory before the user authorizes that phase.
